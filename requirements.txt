# Particle Flow Filters - Dependencies
# Install with: pip install -r requirements.txt

# Core ML framework (includes numpy as dependency)
tensorflow>=2.16.0,<3.0.0
tensorflow-probability[tf]>=0.24.0,<1.0.0  # [tf] extra includes tf_keras

# Visualization
matplotlib>=3.9.0,<4.0.0

# Utilities
tqdm>=4.65.0,<5.0.0
PyYAML>=6.0.0,<7.0.0

# Optional: GPU support for Mac
# tensorflow-metal>=1.0.0  # Uncomment for Apple Silicon GPU

# Note: numpy is a transitive dependency of tensorflow
# Note: scipy is NOT required - all code uses TensorFlow only
